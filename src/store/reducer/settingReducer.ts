// import {
//   INCREASE_TOTAL_LIKES,
//   INCREASE_TOTAL_LIKES_AMOUNT,
//   SET_ROLE,
// } from "../types";

// const initialState = {
//   name: "Admin",
// };

// export const settingReducer = (state = initialState, actions) => {
//   switch (actions.type) {
//     case SET_ROLE:
//       return { ...state, name: actions.payload };

//     default:
//       return state;
//   }
// };
